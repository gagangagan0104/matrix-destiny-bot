# 📦 Настройка GitHub репозитория

## Быстрая инструкция

### 1. Создание репозитория на GitHub

1. Зайдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Название: `матрица-судьбы` или `matrix-destiny` (на английском лучше)
4. Описание: "Telegram бот для расчета личной матрицы судьбы"
5. Выберите Public или Private
6. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
7. Нажмите "Create repository"

### 2. Инициализация Git в проекте

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Matrix Destiny Bot"

# Добавление remote репозитория
git remote add origin https://github.com/ваш-username/матрица-судьбы.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка на GitHub
git push -u origin main
```

### 3. Проверка

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что `.env` и `*.db` файлы НЕ загружены (они в .gitignore)

## ⚠️ Важно!

### Файлы, которые НЕ должны попасть в репозиторий:

- `.env` - содержит секретные токены
- `*.db` - база данных
- `__pycache__/` - кэш Python
- `.venv/` - виртуальное окружение

Все эти файлы уже добавлены в `.gitignore`.

## 🔄 Обновление кода

После изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## 📝 Структура репозитория

После загрузки на GitHub структура должна быть:

```
матрица-судьбы/
├── .gitignore
├── .railwayignore
├── Procfile
├── railway.json
├── README.md
├── SETUP.md
├── RAILWAY_DEPLOY.md
├── requirements.txt
├── run_bot.py
├── run_api.py
├── bot/
├── api/
├── config/
├── database/
├── matrix_calculator/
└── reports/
```

## 🚀 Следующий шаг

После загрузки на GitHub переходите к деплою на Railway:
см. файл `RAILWAY_DEPLOY.md`
