# 🚂 Деплой на Railway

## Быстрая инструкция

### 1. Подготовка GitHub репозитория

```bash
# Инициализация git (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub и выполните:
git remote add origin https://github.com/ваш-username/матрица-судьбы.git
git branch -M main
git push -u origin main
```

### 2. Деплой на Railway

1. Зайдите на [Railway](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит проект

### 3. Настройка переменных окружения

В настройках проекта Railway (Settings → Variables) добавьте:

**Обязательные:**
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

**Опциональные:**
```
ADMIN_IDS=ваш_telegram_id (через запятую для нескольких админов)
DATABASE_URL=sqlite:///./matrix.db (по умолчанию)
RAILWAY_ENVIRONMENT=true (автоматически устанавливается Railway)
```

### 4. Проверка работы

1. Откройте вкладку "Deployments" в Railway
2. Проверьте логи - должны быть сообщения о запуске бота
3. Найдите вашего бота в Telegram и отправьте `/start`

## 📝 Важные моменты

### База данных

- По умолчанию используется SQLite (файл `matrix.db`)
- На Railway файл сохраняется в `/tmp` или используйте Railway PostgreSQL
- Для продакшена рекомендуется PostgreSQL

### Несколько ботов на Railway

Если у вас уже есть другие боты на Railway:
- Каждый бот должен быть в отдельном проекте
- Или используйте разные переменные окружения
- Убедитесь, что токены не конфликтуют

### Мониторинг

- Логи доступны в панели Railway
- Используйте вкладку "Metrics" для мониторинга
- Настройте уведомления в Railway

## 🔧 Решение проблем

### Бот не запускается

1. Проверьте логи в Railway
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен
3. Проверьте формат переменных окружения

### Ошибки с базой данных

1. Убедитесь, что у процесса есть права на запись
2. Для продакшена используйте PostgreSQL:
   - Добавьте PostgreSQL сервис в Railway
   - Используйте переменную `DATABASE_URL` из Railway

### Проблемы с зависимостями

1. Проверьте `requirements.txt`
2. Railway автоматически установит зависимости
3. Если проблемы - проверьте логи сборки

## 🔄 Обновление

После обновления кода в GitHub:
1. Railway автоматически пересоберет проект
2. Или нажмите "Redeploy" в панели Railway
3. Проверьте логи после обновления

## 📊 Мониторинг

- **Логи**: Вкладка "Deployments" → выберите деплой → "View Logs"
- **Метрики**: Вкладка "Metrics"
- **Переменные**: Settings → Variables

## 💡 Советы

1. Используйте PostgreSQL для продакшена
2. Настройте автоматические бэкапы
3. Мониторьте использование ресурсов
4. Используйте Railway CLI для локальной разработки

## 🎉 Готово!

Ваш бот должен работать на Railway. Проверьте его в Telegram!
