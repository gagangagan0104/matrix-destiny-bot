# 🔑 Исправление ошибки токена

## Проблема

```
telegram.error.InvalidToken: The token `8459907406:AAFN1LzWU8K0hXVwdTi_YF7oHU4NLm8-MSk` was rejected by the server.
```

Это означает, что **токен неверный или не установлен** в Railway.

## Решение

### 1. Получите правильный токен

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите "API Token"
5. Скопируйте токен

### 2. Установите токен в Railway

1. Откройте ваш проект на Railway
2. Перейдите в **Settings** → **Variables**
3. Найдите переменную `TELEGRAM_BOT_TOKEN`
4. **Обновите значение** на новый токен
5. Или создайте новую переменную, если её нет:
   - Name: `TELEGRAM_BOT_TOKEN`
   - Value: `ваш_новый_токен_от_BotFather`

### 3. Перезапустите деплой

После обновления переменной:
- Railway автоматически перезапустит контейнер
- Или нажмите **"Redeploy"** вручную

### 4. Проверка

После перезапуска в логах должно быть:
- ✅ "Бот запущен..."
- ✅ "Application started"
- ✅ Успешный запрос `getMe` (200 OK)
- ❌ НЕ должно быть `401 Unauthorized`

## ⚠️ Важно

- Токен должен быть **полным** (включая префикс с номером бота)
- Формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
- Не должно быть пробелов или лишних символов
- Токен должен быть **активным** (бот не удален)

## 🔒 Безопасность

- **НЕ** коммитьте токен в код
- Храните токен только в переменных окружения Railway
- Если токен скомпрометирован, создайте новый через @BotFather
