# Личная Матрица Судьбы - Автоматизированная Система

Комплексное решение для автоматизации расчета и предоставления личной матрицы судьбы клиентам.

## 🚀 Быстрый старт на Railway

### 1. Подготовка проекта

1. Создайте репозиторий на GitHub
2. Загрузите код в репозиторий

### 2. Деплой на Railway

1. Зайдите на [Railway](https://railway.app)
2. Создайте новый проект
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит проект

### 3. Настройка переменных окружения

В настройках проекта Railway добавьте переменные:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id (опционально, через запятую для нескольких)
DATABASE_URL=sqlite:///./matrix.db (по умолчанию)
```

**Как узнать свой Telegram ID:**
- Найдите бота @userinfobot в Telegram
- Отправьте ему сообщение
- Он покажет ваш ID

### 4. Запуск

Railway автоматически запустит бота после деплоя. Проверьте логи в панели Railway.

## 📋 Локальная разработка

### Установка

```bash
pip install -r requirements.txt
```

### Настройка

Создайте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
ADMIN_IDS=your_telegram_id
DATABASE_URL=sqlite:///./matrix.db
```

### Запуск

```bash
python run_bot.py
```

## 🎯 Возможности

- 🤖 **Telegram бот** с улучшенным UX
- 📊 **Автоматический расчет** матрицы
- 🔐 **Админ-панель** с статистикой
- ⭐ **Система обратной связи**
- 📈 **Аналитика и статистика**
- 💾 **База данных** для хранения клиентов
- 📄 **Генерация отчетов** и визуализация

## 📁 Структура проекта

```
.
├── bot/                    # Telegram бот
│   ├── main.py            # Главный файл бота
│   └── admin_panel.py     # Админ-панель
├── api/                    # REST API (FastAPI)
├── matrix_calculator/      # Модуль расчета матрицы
├── database/               # Модели базы данных
├── reports/                # Генерация отчетов
├── config/                 # Конфигурация
├── Procfile               # Для Railway
├── railway.json           # Конфигурация Railway
└── requirements.txt       # Зависимости
```

## 🔐 Админ-панель

Для доступа к админ-панели:
1. Добавьте свой Telegram ID в `ADMIN_IDS`
2. Используйте команду `/admin` в боте
3. Или кнопку "🔐 Админ-панель" в главном меню

## 📚 Документация

- `SETUP.md` - Подробная инструкция по установке
- Код хорошо документирован

## 🛠️ Использование

### Telegram бот
1. Найдите бота в Telegram
2. Отправьте `/start`
3. Следуйте инструкциям

### API
API доступен по адресу `http://localhost:8000` (если запущен локально)
Документация: `http://localhost:8000/docs`

## 📝 Примечания

- База данных создается автоматически при первом запуске
- Для продакшена рекомендуется использовать PostgreSQL вместо SQLite
- Все настройки через переменные окружения

## 🔄 Обновление на Railway

После обновления кода в GitHub:
1. Railway автоматически пересоберет проект
2. Или нажмите "Redeploy" в панели Railway

## 📞 Поддержка

При возникновении проблем проверьте логи в Railway или локально.
