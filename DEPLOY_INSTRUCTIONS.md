# 🚀 Инструкции по деплою

## ✅ Что уже сделано

1. ✅ Проект очищен и готов
2. ✅ Git инициализирован
3. ✅ Коммит создан
4. ✅ Все файлы подготовлены для Railway

## 📦 Следующие шаги

### 1. Создайте репозиторий на GitHub

Я уже открыл страницу создания репозитория в браузере. Если репозиторий еще не создан:

1. Убедитесь, что в поле "Repository name" введено: `matrix-destiny-bot`
2. Описание уже введено
3. Выберите Public или Private
4. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
5. Нажмите "Create repository"

### 2. Подключите локальный репозиторий к GitHub

После создания репозитория на GitHub, выполните в терминале:

```bash
cd "/Users/lutifer/Desktop/матрица/матрица курсор"

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/matrix-destiny-bot.git

# Отправьте код
git branch -M main
git push -u origin main
```

### 3. Деплой на Railway

1. Откройте [Railway](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите репозиторий `matrix-destiny-bot`
5. Railway автоматически определит проект

### 4. Настройте переменные окружения

В настройках проекта Railway (Settings → Variables) добавьте:

**Обязательные:**
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

**Опциональные:**
```
ADMIN_IDS=ваш_telegram_id (через запятую для нескольких)
DATABASE_URL=sqlite:///./matrix.db
```

### 5. Проверка

1. Откройте вкладку "Deployments" в Railway
2. Проверьте логи - должны быть сообщения о запуске бота
3. Найдите вашего бота в Telegram и отправьте `/start`

## 🎉 Готово!

Ваш бот должен работать на Railway!

## 📝 Примечания

- Если у вас уже есть другие боты на Railway, каждый должен быть в отдельном проекте
- База данных SQLite будет работать, но для продакшена рекомендуется PostgreSQL
- Все секреты хранятся в переменных окружения Railway
