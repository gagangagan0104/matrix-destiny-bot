# 🔧 Исправление конфликта на Railway

## Проблема

```
Conflict: terminated by other getUpdates request; make sure that only one bot instance is running
```

Это означает, что **два экземпляра бота** пытаются получать обновления одновременно.

## Решение

### 1. Остановите локальный бот (если запущен)

Если вы запускали бота локально, остановите его:
- Нажмите `Ctrl+C` в терминале
- Или закройте процесс

### 2. Проверьте другие серверы

Убедитесь, что бот не запущен на других серверах или платформах (Heroku, Render, другой Railway проект и т.д.)

### 3. Исправления уже применены

✅ Добавлен `per_message=True` в ConversationHandler (исправлено предупреждение)
✅ Код обновлен на GitHub

### 4. Railway автоматически пересоберет

После push Railway автоматически пересоберет проект. Конфликт должен исчезнуть, если:
- Локальный бот остановлен
- Нет других запущенных экземпляров

## Проверка

После пересборки на Railway проверьте логи - должно быть:
- ✅ "Бот запущен..."
- ✅ "Application started"
- ❌ НЕ должно быть ошибок "Conflict"

## Если проблема сохраняется

1. В Railway: Settings → Variables
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен правильно
3. Перезапустите деплой в Railway (Redeploy)
