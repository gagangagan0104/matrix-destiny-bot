# Инструкция по установке и настройке

## Локальная разработка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или используйте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # На macOS/Linux
# или
venv\Scripts\activate  # На Windows

pip install -r requirements.txt
```

### 2. Создание Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Сохраните токен

### 3. Настройка конфигурации

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id (опционально)
DATABASE_URL=sqlite:///./matrix.db
```

**Как узнать свой Telegram ID:**
- Найдите бота @userinfobot в Telegram
- Отправьте ему сообщение
- Он покажет ваш ID

### 4. Инициализация базы данных

База данных создастся автоматически при первом запуске.

### 5. Запуск

```bash
python run_bot.py
```

## Деплой на Railway

См. файл `RAILWAY_DEPLOY.md` для подробных инструкций.

## Использование

### Telegram бот

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Следуйте инструкциям для ввода данных
4. Получите расчет матрицы судьбы

### Админ-панель

1. Добавьте свой Telegram ID в `ADMIN_IDS` в `.env`
2. Используйте команду `/admin` в боте
3. Или кнопку "🔐 Админ-панель" в главном меню

## Структура базы данных

База данных SQLite создается автоматически в файле `matrix.db`.

Таблицы:
- `clients` - информация о клиентах
- `matrix_calculations` - история расчетов
- `feedbacks` - обратная связь от клиентов

## Решение проблем

### Ошибка импорта модулей

Убедитесь, что вы находитесь в корневой директории проекта и все модули установлены.

### Ошибка подключения к Telegram

Проверьте правильность токена в файле `.env`.

### Ошибки с базой данных

Убедитесь, что у вас есть права на запись в директорию проекта.

## Дополнительные возможности

### Настройка интерпретаций

Вы можете редактировать файл `matrix_calculator/interpretations.py` для изменения текстов интерпретаций.

### Кастомизация визуализации

Настройки визуализации матрицы находятся в `reports/generator.py`.
