# 🌐 Руководство по сбору информации с веб-сайтов

## Обзор

Бот теперь может автоматически собирать информацию с нескольких веб-сайтов, обрабатывать её и объединять в единый полноценный отчет с выводами и изображениями.

## 🏗️ Архитектура системы

### 1. **WebScraper** (`data_collector/web_scraper.py`)
Модуль для сбора информации с веб-сайтов:
- Асинхронный сбор данных
- Извлечение текста по CSS-селекторам
- Извлечение изображений
- Очистка и форматирование текста

### 2. **TextProcessor** (`data_collector/text_processor.py`)
Модуль для обработки текста:
- Удаление дубликатов
- Извлечение ключевой информации
- Объединение текстов из разных источников
- Форматирование для отчетов

### 3. **ImageProcessor** (`data_collector/image_processor.py`)
Модуль для обработки изображений:
- Скачивание изображений
- Изменение размера
- Создание информационных изображений
- Объединение нескольких изображений

### 4. **ReportGenerator** (обновлен)
Интегрирует все модули для создания расширенных отчетов.

## 📝 Настройка источников

### Добавление нового источника

Откройте файл `data_collector/web_scraper.py` и добавьте новый источник в список `MATRIX_SOURCES`:

```python
MATRIX_SOURCES = [
    {
        'name': 'gadalkindom',
        'url': 'https://gadalkindom.ru/matritsa-sudby',
        'selectors': [
            'article',
            '.content',
            'main',
            'p',
            'h1', 'h2', 'h3'
        ]
    },
    {
        'name': 'новый_источник',
        'url': 'https://example.com/matrix',
        'selectors': [
            'article',
            'main',
            '.content',
            'p'
        ]
    }
]
```

### CSS-селекторы

Используйте CSS-селекторы для точного извлечения нужного контента:
- `article` - основной контент статьи
- `.content` - элемент с классом content
- `main` - основной контент страницы
- `p` - все параграфы
- `h1, h2, h3` - заголовки

## 🔧 Использование

### Автоматическое использование

Система автоматически активируется при расчете матрицы. Бот:
1. Собирает информацию со всех настроенных источников
2. Обрабатывает и объединяет тексты
3. Извлекает релевантные изображения
4. Добавляет всё в отчет

### Отключение веб-скрапинга

Если нужно отключить сбор информации с сайтов:

```python
report_generator = ReportGenerator(enable_web_scraping=False)
```

## 📊 Что собирается

### Текстовая информация
- Основной контент страниц
- Релевантная информация по ключевым словам
- Уникальный контент (дубликаты удаляются)

### Изображения
- Все изображения со страниц
- Автоматическое изменение размера
- Максимум 5 изображений на отчет

### Обработка
- Удаление дубликатов
- Извлечение ключевой информации
- Форматирование для читаемости
- Создание резюме

## 🎯 Ключевые слова для поиска

Система автоматически ищет информацию по следующим ключевым словам:
- матрица судьбы
- нумерология
- аркан
- кармическая задача
- число судьбы
- личное число
- путь жизни
- выражение
- душа
- личность

## 📸 Работа с изображениями

### Автоматическая обработка
- Скачивание с сайтов
- Изменение размера до 800x600
- Конвертация в JPEG (качество 85%)
- Отправка в отчет

### Создание информационных изображений
Система может создавать изображения с текстом для лучшей визуализации информации.

## ⚙️ Настройки

### Таймауты
По умолчанию:
- Таймаут запроса: 30 секунд
- Максимум изображений: 5
- Максимальная длина текста: 5000 символов

### Изменение настроек

В файле `data_collector/web_scraper.py`:
```python
timeout=aiohttp.ClientTimeout(total=30)  # Измените таймаут
```

В файле `data_collector/text_processor.py`:
```python
def merge_texts(self, texts: List[str], max_length: int = 5000):  # Измените длину
```

## 🚨 Обработка ошибок

Система автоматически обрабатывает ошибки:
- Недоступные сайты пропускаются
- Ошибки логируются
- Бот продолжает работу даже при сбоях

## 📈 Производительность

### Асинхронная обработка
Все запросы выполняются параллельно для максимальной скорости.

### Кэширование
Рекомендуется добавить кэширование для часто запрашиваемых страниц.

## 🔒 Безопасность

### User-Agent
Система использует стандартный User-Agent браузера для избежания блокировок.

### Rate Limiting
Рекомендуется добавить задержки между запросами для избежания блокировок:
```python
await asyncio.sleep(1)  # Задержка 1 секунда
```

## 📝 Примеры использования

### Добавление нового источника

```python
# В data_collector/web_scraper.py
MATRIX_SOURCES.append({
    'name': 'my_source',
    'url': 'https://example.com/matrix-info',
    'selectors': ['article', '.main-content']
})
```

### Ручной сбор информации

```python
async with WebScraper() as scraper:
    result = await scraper.scrape_site(
        'https://example.com',
        selectors=['article', 'main']
    )
    print(result['text'])
```

### Обработка текста

```python
processor = TextProcessor()
merged = processor.merge_texts(['текст1', 'текст2'])
summary = processor.create_summary(['текст1', 'текст2'], ['ключевое слово'])
```

## 🎨 Результат

После сбора и обработки информации отчет содержит:
- ✅ Базовую информацию о матрице
- ✅ Дополнительную информацию из источников
- ✅ Краткое резюме
- ✅ Детальную информацию
- ✅ Релевантные изображения

Все это автоматически форматируется и добавляется в отчет!
