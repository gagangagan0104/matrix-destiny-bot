# ✅ Система сбора данных с веб-сайтов - Готова!

## 🎉 Что реализовано

### 1. **Модуль веб-скрапинга** (`data_collector/web_scraper.py`)
- ✅ Асинхронный сбор информации с нескольких сайтов
- ✅ Извлечение текста по CSS-селекторам
- ✅ Извлечение изображений
- ✅ Очистка и форматирование текста

### 2. **Модуль обработки текста** (`data_collector/text_processor.py`)
- ✅ Удаление дубликатов
- ✅ Извлечение ключевой информации
- ✅ Объединение текстов из разных источников
- ✅ Создание резюме
- ✅ Форматирование для отчетов

### 3. **Модуль обработки изображений** (`data_collector/image_processor.py`)
- ✅ Скачивание изображений с сайтов
- ✅ Изменение размера
- ✅ Создание информационных изображений
- ✅ Объединение нескольких изображений

### 4. **Интеграция в бота**
- ✅ Автоматический сбор информации при расчете матрицы
- ✅ Объединение информации в единый отчет
- ✅ Отправка изображений вместе с отчетом

## 📋 Как добавить новый источник

### Шаг 1: Откройте файл `data_collector/config.py`

### Шаг 2: Добавьте новый источник в список `MATRIX_SOURCES`:

```python
MATRIX_SOURCES = [
    {
        'name': 'gadalkindom',
        'url': 'https://gadalkindom.ru/matritsa-sudby',
        'selectors': ['article', '.content', 'main'],
        'enabled': True
    },
    {
        'name': 'новый_источник',
        'url': 'https://example.com/matrix',
        'selectors': ['article', 'main', 'p'],
        'enabled': True
    }
]
```

### Шаг 3: Настройте CSS-селекторы

Используйте селекторы для точного извлечения контента:
- `article` - основная статья
- `.content` - элемент с классом content
- `main` - основной контент
- `p` - параграфы
- `h1, h2, h3` - заголовки

## 🔧 Настройки

### Отключение веб-скрапинга

В файле `bot/main.py`:
```python
report_generator = ReportGenerator(enable_web_scraping=False)
```

### Изменение таймаутов

В файле `data_collector/config.py`:
```python
PROCESSING_CONFIG = {
    'request_timeout': 30,  # Измените на нужное значение
    'max_images': 5,        # Максимум изображений
    'max_text_length': 5000 # Максимальная длина текста
}
```

## 📊 Что происходит при расчете

1. **Расчет матрицы** - вычисляются все числа
2. **Сбор информации** - параллельный запрос ко всем источникам
3. **Обработка текста** - удаление дубликатов, извлечение ключевой информации
4. **Обработка изображений** - скачивание и изменение размера
5. **Объединение** - создание единого отчета
6. **Отправка** - пользователь получает полный отчет с изображениями

## 🎯 Результат

Пользователь получает:
- ✅ Базовую информацию о матрице
- ✅ Дополнительную информацию из источников
- ✅ Краткое резюме
- ✅ Детальную информацию
- ✅ Релевантные изображения (до 5 штук)

## 📝 Пример конфигурации

```python
# data_collector/config.py

MATRIX_SOURCES = [
    {
        'name': 'gadalkindom',
        'url': 'https://gadalkindom.ru/matritsa-sudby',
        'selectors': ['article', '.content', 'main'],
        'enabled': True
    },
    {
        'name': 'source2',
        'url': 'https://another-site.com/matrix',
        'selectors': ['article', 'main'],
        'enabled': True
    }
]
```

## ⚠️ Важные замечания

1. **Соблюдайте правила сайтов** - не злоупотребляйте запросами
2. **Добавьте задержки** - если сайты блокируют, добавьте `await asyncio.sleep(1)`
3. **Проверяйте доступность** - система автоматически обрабатывает ошибки
4. **Кэширование** - рекомендуется добавить кэш для часто запрашиваемых страниц

## 🚀 Готово к использованию!

Система полностью интегрирована и готова к работе. Просто добавьте нужные источники в конфигурацию!
